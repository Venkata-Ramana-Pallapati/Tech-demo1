apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend-deployment114
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
      - name: frontend
        image: venkat345/frontend-demo1234:latest
        ports:
        - containerPort: 80
        imagePullPolicy: IfNotPresent # or Always
